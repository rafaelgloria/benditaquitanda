<%= form_for(@usuariocompleto) do |f| %>
  <% if @usuariocompleto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@usuariocompleto.errors.count, "error") %> prohibited this usuario from being saved:</h2>

      <ul>
      <% @usuariocompleto.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-horizontal">
    <div class="form-group">
      <%= f.label :email, class: 'col-sm-2 control-label' %>
      <div class="col-sm-8">
        <%= f.text_field :email, class: 'form-control' %>
      </div>
    </div>
    <div class="form-group">
      <div class="col-sm-offset-2 col-sm-10">
        <%= f.submit class: 'btn btn-default' %>
      </div>
    </div>
  </div>
<% end %>
